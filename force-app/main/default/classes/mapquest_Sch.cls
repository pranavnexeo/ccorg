global class mapquest_Sch //implements schedulable
{
   /* global void execute(SchedulableContext sc){
        mapquest_batch r =new mapquest_Batch('');
        id idbatch = database.executebatch(r, 25);
    }  */
}