@isTest

private class sch_owner_updates_Test {
    public static testMethod void testme() {
    
    SchedulableContext sc;
    
    sch_owner_updates so = new sch_owner_updates();
    so.execute(sc);

   }
   
}